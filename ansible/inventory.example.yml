# Example inventory for compute-service nodes
all:
  hosts:
    compute-node-1:
      ansible_host: 192.0.2.10  # replace with real address or hostname
      ansible_user: ubuntu
      # any host-specific vars may be added here

  children:
    compute_nodes:
      hosts:
        compute-node-1: {}
